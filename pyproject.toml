[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
    "pyusb",
    "pycryptodome",
    "colorama",
    "shiboken6", 
    "pyside6",
    "mock"
]
